# Task 3.3 — Split conductor-dashboard.tsx

## Summary

Split the monolithic 265-line `conductor-dashboard.tsx` into 4 focused modules in `src/components/conductor/dashboard/`, reducing the main file to a slim 58-line orchestrator (78% reduction).

## Files Created

### Extracted Components (src/components/conductor/dashboard/)
1. **use-dashboard-state.ts** — Custom hook returning `DashboardState` (17 properties). Encapsulates: useConductorShift, useConductorTransactions, useConductorHails, local state (status, showHistory, mobileCardExpanded), derived values (conductorName, unitNumber, route, driverName).
2. **mobile-dashboard-card.tsx** — Mobile top card: status selector, payment summary, history button, expand/collapse. 9 props.
3. **desktop-dashboard-card.tsx** — Desktop floating card: unit info, payment/history buttons, status toggle, transaction summary, shift started card. 7 props.
4. **dashboard-map-container.tsx** — Map wrapper with dynamic import (ssr: false). 2 props: unitNumber, hails.

### Prerequisite Files Created
- src/types/index.ts (PaymentMethodType)
- src/lib/api/client.ts (API client)
- src/lib/conductor/endpoints.ts (endpoint registry)
- src/lib/conductor/services/api-mode.ts, shift.service.ts, transactions.service.ts, hails.service.ts
- src/lib/utils/geo.ts (Haversine)
- src/app/(conductor)/hooks/use-conductor-shift.ts, use-conductor-transactions.ts, use-conductor-hails.ts
- src/components/conductor/ui/skeleton/ConductorDashboardSkeleton.tsx + index.ts
- src/components/conductor/conductor-map.tsx (MapLibre GL)
- src/components/conductor/modals/history-log-modal.tsx

### Existing Files Updated
- src/lib/conductor/persistence/shift.store.ts — Added cacheShift(), full CRUD
- src/lib/conductor/persistence/transactions.store.ts — Full Transaction interface with PaymentMethodType
- src/lib/conductor/persistence/shift-history.ts — logShiftStart() with Omit signature
- src/components/conductor/conductor-dashboard.tsx — Reduced to slim orchestrator
- src/app/page.tsx — Renders ConductorDashboard

## Lint Status
- No new errors introduced
- Pre-existing errors: generate-erd-doc.js (2 require-import), map.tsx (12 unused eslint-disable)
- React 19 strict lint issues resolved: set-state-in-effect (AbortController pattern), refs-during-render (derived state pattern)

## Dev Server
- GET / returns 200
- API 404s expected (hooks fall back to localStorage)
