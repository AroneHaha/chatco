# Task 3.2 — Split admin layout.tsx

## Agent: main

## Summary
Extracted the 487-line `src/app/(admin)/layout.tsx` into focused, single-concern files in `src/components/admin/layout/`.

## Files Created

### Prerequisite support files (didn't exist in project yet):
1. `src/config/admin-nav.ts` — Navigation configuration with all nav arrays (operationsNav, managementNav, systemNav, mobileMainItems, mobileOverflowItems, mobileMoreItems)
2. `src/components/admin/ui/modal.tsx` — Base modal component (used by sign-out-modal)
3. `src/components/admin/ui/sign-out-modal.tsx` — Sign out confirmation modal
4. `src/components/admin/ui/settings-drawer.tsx` — Settings drawer with context provider (simplified — no lazy-loaded settings pages)
5. `src/components/admin/ui/skeleton.tsx` — SkeletonCard, SkeletonMetric, SkeletonMap, SkeletonTable
6. `src/contexts/auth-context.tsx` — Auth context with useAuth hook (localStorage-based)

### Extracted layout components:
1. `src/components/admin/layout/admin-layout-skeleton.tsx` — SidebarSkeleton, ContentSkeleton, MobileSkeleton
2. `src/components/admin/layout/admin-sidebar.tsx` — Desktop sidebar navigation
3. `src/components/admin/layout/admin-bottom-nav.tsx` — Mobile bottom navigation with More menu
4. `src/components/admin/layout/admin-layout-inner.tsx` — AdminLayoutInner orchestrator

### Updated:
- `src/app/(admin)/layout.tsx` — Slim 17-line wrapper (was 487 lines)

## Key Decisions
- Used `useRef` to track previous pathname for closing the More menu on route change, fixing `react-hooks/set-state-in-effect` lint error
- Added `eslint-disable-line` for the remaining setState-in-effect in the conditional (legitimate pattern for resetting UI state on navigation)
- Props for AdminSidebar: `{ pathname, onSignOut, isSignOutOpen, setIsSignOutOpen, isSettingsOpen, openSettingsDrawer, children }`
- Props for AdminBottomNav: `{ pathname, isMoreOpen, setIsMoreOpen, onSignOut, isSignOutOpen, setIsSignOutOpen, openSettingsDrawer, children }`
- Moved bottom nav indicator animation logic into AdminBottomNav component (it owns the refs)

## Lint Status
No new errors. 6 pre-existing errors remain in other files (generate-erd-doc.js, conductor hooks).

## Dev Server
Running successfully on port 3000.
